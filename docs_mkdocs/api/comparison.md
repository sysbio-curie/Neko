# Network comparison

`compare_networks` compares the node and edge tables of two `Network` objects
and returns their shared and unique topology.

```python
from neko._methods.compare import compare_networks

comparison = compare_networks(network_a, network_b)
```

::: neko._methods.compare.compare_networks
    options:
      show_source: true
      show_root_heading: true
      heading_level: 2
